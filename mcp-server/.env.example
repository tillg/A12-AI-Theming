# Environment configuration for MCP Server
# Copy this file to .env and adjust values as needed

# Frontend URL
FRONTEND_URL=http://localhost:8081

# Backend URL
BACKEND_URL=http://localhost:8082

# Default test credentials
DEFAULT_USERNAME=admin
DEFAULT_PASSWORD=A12PT-admintest

# Browser settings
# Set to 'true' to hide browser for faster execution (default is visible)
HEADLESS=false

# Slow down browser actions by N milliseconds (helpful for watching automation)
SLOW_MO=50
